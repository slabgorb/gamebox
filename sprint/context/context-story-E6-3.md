# Story E6-3 Context

## Title
Clue board geometry + dice movement (grid/rooms/doors/secret passages, BFS, render harness)

## Metadata
- **Story ID:** E6-3
- **Type:** story
- **Points:** 8
- **Priority:** p2
- **Workflow:** tdd
- **Repo:** g-1
- **Epic:** Clue
- **Depends On:** E6-2 (action reducers complete)

## Problem
Plan 2 (PM writes before Dev picks up): Board geometry traced from docs/Cluedo_board_text.svg: corridor grid, 9 room polygons, door squares, corner secret passages (Kitchen↔Study, Conservatory↔Lounge). Reachable-squares BFS, roll/move actions routing into E6-2's enterRoom reducer, offline rsvg-convert render harness. Dice client-side. Depends on E6-1/E6-2.

## Technical Approach
_Approach hints to be refined by TEA/Dev. The story title above defines the intended behavior._

Implementation Plan: `docs/superpowers/plans/2026-07-01-clue-board-geometry.md` (Plan 2 of 4, 8 tasks):
1. `geometry.js` — grid (24×25), 9 rooms, 17 doors, 2 secret-passage edges, `buildGeometry` factory + non-overlap/adjacency/count/symmetry assertions.
2. Offline `rsvg-convert` render harness (`plugins/clue/tools/render-board.mjs`, pure `buildBoardSvg` + CLI).
3. `movement.js` — reachable-squares self-avoiding walk (orthogonal, **canonical exact-count on corridors**, blocking, no-re-enter, ≤die into rooms with excess pips ignored).
4. Place pawns on canonical start squares + `pendingRoll` in `buildInitialState` (flips Plan-1 "pawns off-board" test — Plan 1 pre-approved this).
5. `roll` action + `pendingRoll` lifecycle (clears on every turn transition).
6. `move` action (corridor destination + room entry via the EXISTING `doEnterRoom` reducer).
7. `secretPassage` action (corner-room leap, no roll).
8. Surface reachable moves in `cluePublicView` for the active seat only.

## Scope
- In scope: board geometry module (grid + 9 rooms + doors + secret passages), offline render harness (rsvg-convert), movement BFS, roll/move/secretPassage actions integrated into the existing 'move' phase, pawns on start squares, reachable-moves surfacing in cluePublicView.
- Out of scope: React client + client-mirror geometry (Plan 4); bots/shortlist (Plan 3); plugin.js manifest + registration (Plan 4).

## Acceptance Criteria
- geometry.js (grid + 9 rooms + doors + 2 secret-passage edges) verified offline via the rsvg-convert render harness with non-overlapping room polygons.
- roll (client-supplied value) + move (grid square) actions enforce orthogonal, no-revisit, no-move-through-occupied movement and route room entry through the existing enterRoom reducer; entering a room ends the move.

---
_Generated by setup for story E6-3 from the sprint YAML and implementation plan._
