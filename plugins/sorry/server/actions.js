// Stub — the real turn engine (move application, bumps, swap, Sorry!, win,
// auto-pass) is implemented in story E3-4.
export function applySorryAction({ state }) {
  return { error: 'not implemented' };
}
